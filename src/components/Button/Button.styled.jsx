import styled from "styled-components"

const Button = styled.button`
    width: 100%;
    padding: 10px;
    border-radius: 5px;
    border: none;
    margin-top: 10px;
    cursor: pointer;
`

const S = {
    Button
}

export default S